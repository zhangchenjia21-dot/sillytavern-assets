---
title: L2-03｜兖豫青徐—中原东部 R5 通过记录
status: passed
created: 2026-08-28
scope: Level 2 重要区域高密版
map_id: L2-03_yanyu_qingxu
gate: R5
base_r4: passed
candidate: v002
---

# L2-03｜兖豫青徐—中原东部 R5 通过记录

## Gate

> **PASSED / READY FOR R6**

R5 v002 已完成 Anchor / Overlay / Data 复核。R4 v002 继续冻结为 PASSED，不重新打开。

## 视觉 / Anchor 通过项

- v001 的“谯位于许东北”错误已修正：v002 中谯位于许东南，并仍位于彭城以西；
- R02 已同步改为 `许 → 谯 → 小沛 → 彭城` 的局部正确控制链；
- “蒙山”标签已从开阳东南错误位置移回开阳西北的鲁中南山地区域；
- 其余城市 Anchor 未被无必要重排；
- 黄河渡河、泰山北路 / 南路、彭城—下邳、四州低对比背景等既有通过关系保持成立；
- 正式中文、城市层级、Narrative 符号、道路层级与 2K 可读性达到当前 Gate 要求。

## SVG / JSON 同构检查

- SVG 为独立 `2560×1440` Overlay，不内嵌 R4 底图；
- A01–A22 canonical ID 已与冻结对象表一致；
- N01–N05 与路线引用均存在；
- R01–R08 以及 `R03b` 均在 SVG 与 JSON 中有对应记录；
- SVG 路径控制点与 JSON route control_points 一致；
- JSON `u/v` 与 canonical `x/y` 坐标一致；
- `historical_parent` 已恢复完整 `州·郡国`；
- `display_role` 已恢复 core / narrative / route / gateway 等职责；
- `historical_status` 与 `regional_anchor_status` 已拆分；Narrative 说明移入 notes。

## Data Owner 规范化

复核时发现 v002 JSON 仍有 5 个可确定、无需图像返工的数据字段偏差：

1. 陈留 historical_status 应继承 Level 1 `LOCKED`；
2. 彭城 historical_status 应为 `LOCKED`；
3. 下邳 historical_status 应为 `LOCKED`；
4. 寿春 historical_status 应为 `LOCKED`；
5. 寿春 historical_identity 按当前 Data Owner 保持 `寿春`，不自行扩写为 `寿春县`。

这些属于 Data Owner 确定性规范化，不影响 v002 图像 / SVG。Technical Art / Data Owner 已生成规范化 canonical JSON：

`L2-03_兖豫青徐_anchors_v002_canonical.json`

后续应以该规范化版本作为数据交接基准。

## Soft Issues

- 乌巢仍属 Optional；当前没有造成不可接受碰撞，可保留；
- 道路虚线略偏开发图语言，但在当前正式图中仍可接受，不构成返工理由；
- 郡国背景保持克制，部分对比偏低属于设计目标，不阻塞。

## 冻结边界

R5 通过后，除新的明确 Hard Blocker 或 Owner superseding decision 外，不再移动已通过 Anchor 或重新排正式 Overlay。

R6 只负责：

- 最终 QA；
- 文件完整性 / 命名 / 2K 输出；
- 正式资产卫生；
- Owner Acceptance；
- Gate / 交接记录。

## Next

> **READY FOR R6｜Final QA / Owner Acceptance**
