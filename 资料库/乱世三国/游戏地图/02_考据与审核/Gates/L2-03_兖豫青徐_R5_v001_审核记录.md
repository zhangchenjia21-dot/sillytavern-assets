---
title: L2-03｜兖豫青徐—中原东部 R5 v001 审核记录
status: failed-needs-targeted-revision
created: 2026-08-28
scope: Level 2 重要区域高密版
map_id: L2-03_yanyu_qingxu
gate: R5
base_r4: passed
candidate: v001
---

# L2-03｜兖豫青徐—中原东部 R5 v001 审核记录

## Gate

> **FAIL｜TARGETED OVERLAY / DATA REVISION ONLY**

R4 v002 继续冻结并保持 PASSED。当前失败只发生在 R5 Anchor / Overlay / Data 层，禁止重新生成或修改底图。

## 已通过部分

- 正式图确实以已通过 R4 底图为基础，未发现结构性重生成迹象；
- 城市 / Narrative / 道路 / 行政背景 / 自然文字的总体层级成立；
- 许—陈留—濮阳、彭城—下邳、泰山北路 / 南路、黄河渡河等大多数主要关系可读；
- 白马 / 延津 / 黎阳与黄河关系基本成立；
- 兖州 / 豫州 / 徐州 / 青州四州低对比背景策略成立；
- 正式文字为真实中文，未见伪字、水印或平台标记；
- SVG 为独立 2560×1440 Overlay，未内嵌底图；JSON 也使用 2560×1440 canonical 坐标。

## Hard Blocker 01｜谯 Anchor 位于许的错误方向

冻结拓扑要求：

> 谯位于许的东南方向，并位于彭城以西。

v001 JSON：

- 许：`(410, 985)`
- 谯：`(750, 900)`

在画布 Y 向下的坐标系中，谯实际位于许的东北方向，违反冻结拓扑。

处理：只移动谯的 Regional Anchor / label，并相应修正 R02；不得移动许或修改 R4 底图。

## Hard Blocker 02｜蒙山标签落在错误山地区域

v001 将“蒙山”放在开阳明显东南侧的东部丘陵。该关系会误导鲁中南地貌认知；蒙山应回到开阳西北方向的鲁中南山地 / 蒙山山系语义区域。

处理：只移动自然地理文字；不重画山体。

## Data Contract Blocker

当前 JSON / SVG 虽可用，但没有遵守已下发的 canonical 对象 ID 与字段职责：

1. `A04–A21` 多个 ID 与冻结对象表错位；
2. `historical_parent` 多数只写郡国，未保留 `州·郡国` 完整历史 parent；
3. 鄄城被写为 `东郡`，应按当前 Data Owner 为 `兖州·济阴郡`；
4. `display_role` 被压缩成通用 `city`，丢失 core / narrative / route / gateway 等职责；
5. `status` 被写成剧情说明文字，混淆历史定位 / Anchor 状态与 Narrative notes；
6. SVG 存在 `R03b` 延津支路，但 JSON routes 未记录该 branch，Overlay 与数据包不完全同构。

这些问题必须确定性修正，但不需要重新排整个正式图。

## Soft Issues

- 乌巢当前显示仍可接受；若修订后发生碰撞，可按原规则隐藏；
- 少数郡国背景对比偏低，但 2K 仍属可接受范围；
- 道路虚线略显技术化，但不构成阻塞。

## Revision Boundary

v002 只允许：

- 修正谯 Anchor / label / R02 局部控制点；
- 移动蒙山文字；
- 修正 JSON canonical ID / historical_parent / display_role / status 等字段；
- 使 `R03b` 或等价 branch 在 JSON 中有明确数据记录；
- 必要的局部文字避让。

禁止：

- 重新生成 R4；
- 改黄河 / 淮水 / 泗水 / 山体 / 海岸；
- 重排其它已通过城市；
- 为局部修正重做整张图。

## Next

> **R5 v001 FAIL → KIMI targeted v002 → R5 re-review**
