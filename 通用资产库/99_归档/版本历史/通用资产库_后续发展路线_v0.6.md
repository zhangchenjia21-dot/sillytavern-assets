---
title: 通用资产库｜后续发展路线
version: 0.6
status: current-planning-roadmap
created: 2026-08-17
updated: 2026-08-18
supersedes:
  - 通用资产库_后续发展路线_v0.5
---

# 通用资产库｜后续发展路线 v0.6

# 1. 当前状态

当前 Existing Reusable Expansion：

```text
11
```

其中：

- 稳定语义基线：8；
- 通用超自然候选：3。

新增：

- EP-REPUTATION-CORE｜名望与社会评价核心 v0.1；
- ORG + Reputation Context Cluster Audit PASS；
- Runtime Context Contract Pattern v0.4 已冻结；支持 embedded + unique sidecar 两种 Canonical Contract location。

---

# 2. 当前最高优先级｜Existing Expansion Context Retrofit

Wave 1 已完成：

```text
Character sidecar PASS
Relationship sidecar PASS
Health sidecar PASS
Combat sidecar PASS
ORG v0.1.2 PASS
→ Wave 1 Context Cluster Audit PASS
```

Wave 2 已完成：

```text
Survival sidecar PASS
Traveler/System sidecar PASS
→ Wave 2 Cluster Audit PASS
```

已完成：

```text
Wave 3
Magic
→ Divine
→ Generic Identity Normalization
→ Wave 3 Cluster Audit
✓ PASS / CLOSED
```

当前 NEXT：**Wave 4 Combat Magic + Generic Identity Normalization → Generic Library Context Convergence**。

---

# 3. Context Retrofit 通过后的 Shared Foundation 顺序

```text
EP-KINSHIP-CORE
↓
EP-POLITICS-CORE + Han Migration
↓
EP-ECONOMY-CORE + Han Migration
↓
EP-WAR-CORE + Han Migration
↓
Generic Library Convergence
```

Governance / Law 继续作为重要 P1 Consumer 暂缓。

---

# 4. Runtime Context Orchestration｜所有后续资产前提

冻结：

```text
Available != Enabled != Runtime Relevant != Model Visible
Full Asset != Prompt Payload
Dependency Graph != Context Inclusion Graph
```

Router：

```text
Player Input
+
Enabled Routing Profiles
+
minimal current context
↓
Model-first Semantic Router
↓
immediate Domain / Intent candidates
↓
Program structural validation + state-mandatory augmentation
↓
JIT bounded projection
```

后续后果通过 Formal Event / Handoff 激活，不要求 Router 预测全世界因果。

Pattern：`[[通用资产库_RuntimeContextContract模式_v0.4]]`；Contract Index：`[[通用资产库_RuntimeContextContract索引_v0.3]]`。

---

# 5. Retrofit 的验证目标

Wave 1 / Wave 2 / Wave 3 已完成并 PASS，当前已验证：

- 普通闲聊；
- Character capability 使用；
- Relationship 社交；
- Combat + Health Handoff；
- ORG / Reputation 同时 Enabled 但无关时不加载；
- Survival background progression 不需要持续模型调用；
- Traveler/System Package / Feature / Module 可以逐层裁剪 Router Directory；
- Magic / Divine 大型 Definition Registry 只按 actor-accessible / authorized / intent-relevant subset 投影；
- `Bounded != Starved`，selected Definition / actor context 必须保留完成职责所需信息；
- durable/interactable Narrative affordance 必须有 authoritative Runtime referent。

全部 Retrofit 完成后做：

```text
Typical Valid Bundle
Heavy Valid Bundle
Worst Reasonable Valid Bundle
```

核心趋势：

```text
Enabled Assets ↑↑↑
ordinary unrelated Turn context ≈ stable
```

```text
Session Length ↑↑↑
ordinary Turn context ≈ bounded
```

---

# 6. Generic Library Identity Normalization

当前：

- Magic：**PASS / normalized current workspace binding**；
- Divine：**PASS / normalized current workspace binding**；
- Combat Magic：**Wave 4 NEXT**。

Magic / Divine 正文的 Canonical Mechanism 已是跨世界 Generic；诸界余辉只保留 reference consumer / compatibility example。

成熟大正文中的旧 `skill / blueprint` 字段按 authoring provenance / legacy backlink 解释，不为纯管理元数据机械重写正文；current governance binding 由当前 Matrix / Blueprint / Maintenance / Context Index / current Skill 决定。

Wave 4 对 Combat Magic 同批执行 Context Retrofit + Identity Normalization，避免重复版本 churn。

---

# 7. Future Magic Theme Slots

仍按真实需求再生产：空间与位面、预言与命运、元素与毁灭、心灵与幻象、灵魂与亡灵、生命与变形、法术研究与创造、魔法工艺与魔具。

不得为了“库看起来完整”提前填槽。

---

# 8. 暂缓 Supporting Domains

当前不主动抽：医学专门玩法、毒素 /炼金、制造 /工艺、载具 /航海、商业专门玩法、科举、教育、宗教组织、地产、犯罪、情报、工程、派系。

除非真实消费者证明现有 Owner 无法承载。

---

# 9. G9 Rebinding Priority

G9 按上游 / Context 风险，不按资产数量推进。

建议：

```text
Context Retrofit stable foundations
↓
Remaining Shared Foundation
↓
Generic Gameplay / Framework
↓
Supernatural Core
↓
Theme
```

实际顺序服从真实 Host / Runtime Capability。

---

# 10. Architecture Reflection

每个 Retrofit Wave / Shared Foundation Cluster 结束后执行 Future Explosion Review：

- Context 是否随无关资产增长；
- 是否出现 state mirroring；
- 是否误把依赖展开成 Prompt；
- 是否过度拆模块；
- 是否需要用户做成本 /长期体验反思。

---

# 11. G8 / G9 Freshness Boundary

当前最新事实：

```text
WEB-04 Host              PASS / CLOSED
WEB-05 Creation Migration PASS / CLOSED
WEB-08 Multi-action       PASS / CLOSED
Engineering Gate          historical PASS
Stage UAT                 FAIL / BLOCKED
G8                        ACTIVE / UAT FIX / REOPENED
Current Next              G8-UAT-01 v1.1
G9                        NOT AUTHORIZED
```

当前 G8 已进入 `G8-UAT-01 v1.1｜Playable Runtime Seed + Narrative Authority + Dynamic Five Suggested Inputs Convergence`；因此 Context Retrofit 仍是 **G9 前 Semantic Preparation**，不是 machine protocol implementation。

---

# 12. 下一动作

当前下一正式资产任务：

> **Existing Expansion Context Retrofit Wave 4：EP-MAGIC-COMBAT + Generic Identity Normalization → Generic Library Context Convergence。**


---

# 13. Wave 3 新增 Runtime Context 原则

```text
Domain Active != Full Definition Registry Visible
Bounded != Starved
```

Magic / Divine 表明大型 Spell / Invocation Registry 必须在 Runtime Definition 层检索，模型只读取当前 selected Definition Projection。

G8 Stage UAT 同时证明：有界上下文不能过薄；Narrative-visible durable/interactable affordance 需要 authoritative Runtime referent。
